#
# This systemd unit starts a nextsec-controller instance using Podman.
# Most parts of this file come from podman-generate-systemd.
#
[Unit]
Description=nextsec-controller server

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=%S/state/environment
WorkingDirectory=%S/state
Restart=always
ExecStartPre=/bin/rm -f %t/nextsec-controller.pid %t/nextsec-controller.ctr-id
ExecStart=/usr/bin/podman run \
    --detach \
    --conmon-pidfile=%t/nextsec-controller.pid \
    --cidfile=%t/nextsec-controller.ctr-id \
    --cgroups=no-conmon \
    --replace --name=%N \
    --publish=127.0.0.1:${TCP_PORT}:8080 \
    ${ECHO_SERVER_IMAGE}
ExecStop=/usr/bin/podman stop --ignore --cidfile %t/nextsec-controller.ctr-id -t 10
ExecStopPost=/usr/bin/podman rm --ignore -f --cidfile %t/nextsec-controller.ctr-id
PIDFile=%t/nextsec-controller.pid
Type=forking

[Install]
WantedBy=default.target
